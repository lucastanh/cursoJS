// write a function called isLandcape that takes 2 arguments, width and height of a image (number).
// returns true if the image is in landscape mode

// ARROW FUNCTION
const isLandscape = (wid, hei) => wid > hei
console.log(isLandscape(1920, 1080))